package site.tellmetodo.todoapp.global.config;

public class UserManagementConfig {
    // UserDetailsManager(사용자 CRUD 작업)
    // UserDetailsService(사용자 검색)

    // UserDetailsService, PasswordEncoder?(예는 PasswordConfig인가?)
}
